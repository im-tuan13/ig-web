<x-app-layout><div class="mx-auto max-w-3xl px-4 py-6"><div class="mb-6 flex justify-between"><h1 class="text-xl font-bold">Call history</h1><a href="{{ route('messages.index') }}" class="text-sky-500">Messages</a></div><div class="divide-y rounded-xl border bg-white">@forelse ($calls as $call) @php $other = $call->caller_id === auth()->id() ? $call->receiver : $call->caller; @endphp<div class="flex items-center gap-3 px-4 py-4"><div class="text-2xl">{{ in_array($call->status, ['declined','missed']) ? '↙' : '↗' }}</div><div class="flex-1"><p class="font-semibold">{{ $other->username }}</p><p class="text-xs text-neutral-500">{{ ucfirst($call->status) }} · {{ $call->created_at->diffForHumans() }}{{ $call->duration ? ' · '.gmdate('i:s', $call->duration) : '' }}</p></div><span>{{ $call->type === 'video' ? '🎥' : '📞' }}</span></div>@empty<p class="p-8 text-center text-sm text-neutral-500">No calls yet.</p>@endforelse</div><div class="mt-5">{{ $calls->links() }}</div></div></x-app-layout>
